# Copy to .env and fill in. .env is gitignored and never committed.

# Used by src/layer2_contract_rules.py to extract rate tables from contracts.
GEMINI_API_KEY=

# Model used for contract rule extraction.
GEMINI_MODEL=gemini-2.0-flash
